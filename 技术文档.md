# 基于 ESP32-P4 的智能家居边缘控制终端

## 摘要

本设计实现了一套基于乐鑫 ESP32-P4 微控制器的智能家居边缘控制终端。系统以 ESP32-P4 为主控芯片，搭载 MIPI DSI 触控显示屏、MIPI CSI 摄像头、ES8311 音频编解码器及多种环境传感器，并配备旋钮编码器和按键作为交互输入。同时外接 ESP32-C3 作为远端传感器节点，通过局域网 HTTP 协议实时上报温湿度和光照数据。

软件方面，系统基于 ESP-IDF 5.5 开发框架，采用 LVGL 9.3 构建了包含聊天界面、智能家居控制面板、传感器历史曲线和天气显示的多页面 UI。系统支持 WebSocket 与 MQTT+UDP 双协议与云端 AI 服务器通信，实现语音识别、大语言模型处理、语音合成的全双工交互。内置 MCP（Model Context Protocol）服务器，使 AI 可通过标准化工具调用接口控制硬件设备。板载摄像头支持本地帧差法移动侦测与肤色检测，检测到人体移动时自动播报警示语音并闪烁灯光。同时集成 OTA 远程固件升级、SoftAP 智能家居 Web 服务器、本地 PCM 语音播报、定时提醒等功能。

**关键词**：ESP32-P4；智能家居；AI 语音交互；LVGL；MCP 协议；边缘计算

---

## 第一部分 作品概述

### 1.1 功能与特性

本系统是一款智能家居边缘控制终端，核心功能如下：

- **AI 语音对话**：支持离线唤醒词唤醒，经云端 ASR 识别、LLM 理解、TTS 语音合成的全双工对话，界面呈现聊天气泡样式
- **智能家居控制**：通过触控屏、旋钮编码器或 AI 语音三种方式控制风扇与灯光，支持自动模式（温度 > 28°C 自动开风扇、光照 < 10 lux 自动开灯）
- **环境监测**：板载 DHT22（温湿度）和 BH1750（光照）实时采集本地环境数据，同时通过 HTTP API 接收 ESP32-C3 远端节点数据，支持 120 点历史曲线显示
- **人体移动侦测**：摄像头通过帧差法检测运动，结合肤色检测定位人脸区域，触发本地语音播报和灯光闪烁
- **天气显示**：通过 wttr.in 接口获取并显示天气预报
- **OTA 远程升级**：支持固件远程更新，含版本检查和回滚保护

### 1.2 应用领域

- **智能家居控制中心**：集环境监测、设备控制、语音交互于一体的桌面智能终端
- **老人/儿童智能看护**：移动侦测自动提醒、定时提醒、AI 语音陪伴
- **边缘计算网关**：在家庭局域网内完成传感器数据汇聚、本地自动化逻辑执行，降低云端依赖

### 1.3 主要技术特点

1. **全双工音频流水线**：基于 **Opus 编解码**的流式语音处理，**60ms 帧长**，支持设备端 AEC 与服务器端 AEC 两种模式
2. **双协议通信**：同时支持 **WebSocket** 和 **MQTT+UDP** 混合协议，UDP 音频通道使用 **AES-CTR 加密**
3. **MCP 标准化工具调用**：实现 **JSON-RPC 2.0** 标准的 MCP 服务器，将硬件设备抽象为标准工具，支持 AI 自动发现和调用
4. **本地智能感知**：摄像头**帧差法移动侦测**结合 RGB 肤色检测，无需云端即可实现人体感知
5. **多模态交互**：同时支持**触控、旋钮编码器、按键、语音**四种交互方式
6. **边缘自治能力**：本地传感器采集 + 自动化控制逻辑 + Web 服务器，**断网时仍可独立运行**

### 1.4 主要性能指标

| 指标项 | 参数 |
|--------|------|
| 主控芯片 | ESP32-P4（双核 400MHz） |
| 内存 | 32MB PSRAM + 2MB SRAM |
| 闪存 | 16MB QIO Flash |
| 显示 | MIPI DSI TFT，GT911 电容触摸 |
| 摄像头 | MIPI CSI OV5647（500 万像素） |
| 音频 | ES8311 编解码器，24kHz 采样，I2S 接口 |
| 传感器 | DHT22（温湿度），BH1750（光照强度） |
| 通信 | WiFi 802.11 b/g/n，2.4GHz |
| 语音帧长 | 60ms |
| 传感器上报周期 | 本地 2s / 远端 5s |
| 移动侦测间隔 | 300ms |
| OTA 升级 | 支持，含回滚保护 |

### 1.5 主要创新点

1. **低成本多模态 AI 终端**：在 MCU 级别平台上实现了**语音对话 + 摄像头视觉 + 触控交互**的融合方案
2. **MCP 协议驱动的设备抽象**：将传统家居设备控制抽象为**标准化 JSON-RPC 2.0 工具接口**，使大语言模型可直接发现和操纵物理设备
3. **本地-云端协同感知**：本地端帧差法 + 肤色检测实现**实时移动侦测（300ms 间隔）**，云端进行 ASR/LLM/TTS 处理，兼顾实时性与智能性
4. **边缘自治设计**：断网时**本地传感器采集、自动化控制逻辑和 Web 面板**仍可独立运行

### 1.6 设计流程

```
需求分析 -> 硬件选型与设计 -> 传感器与外设驱动开发
       \-> 核心框架搭建（Application 状态机）
            -> 音频流水线 / LVGL 显示 / 通信协议
            -> MCP 服务器 / Web 服务器
            -> 远端传感器 / 移动侦测 / OTA
            -> 整机联调与性能优化
```

---

## 第二部分 系统组成及功能说明

### 2.1 整体介绍

系统整体架构分为三层：感知层（传感器、摄像头、麦克风）、控制层（ESP32-P4 主控）、交互层（触控屏、扬声器、LED）以及云端层（AI 语音服务器）。

系统整体框图：

```
                   云端 AI 服务器
                   (ASR + LLM + TTS)
                 /                 \
            WebSocket           MQTT+UDP
                 \                 /
              WiFi (2.4GHz 局域网)
                  |
            ESP32-P4 主控板
    +-------------+-------------+
    | 音频流水线   |  MCP 服务器  |
    | MIC -> Opus |  工具调用    |
    | SPK <- Opus |             |
    +------+------+------+------+
    | DHT22 | BH1750 |  GT911    |
    | 本地   | 光照   |  触摸屏   |
    +------+------+------+------+
    | OV5647 | EC11  |  BOOT按键  |
    | 摄像头  | 旋钮  |          |
    +------+------+------+------+
    | 风扇控制 | 灯光控制 | PWM背光|
    +-----------------------------+
            |
    HTTP POST (局域网)
            |
    ESP32-C3 远端节点
    (DHT22 + BH1750)
```

各模块之间的关系：

- Application 状态机作为系统中枢，协调音频、显示、通信等子模块的生命周期
- 音频流水线使用 FreeRTOS 队列传递 PCM 和 Opus 数据包，分为编码发送和解码播放两条路径
- MCP 服务器通过协议层接收 AI 调用请求，映射到具体硬件控制函数
- Web 服务器运行于 SoftAP 模式，提供 RESTful API 供局域网内浏览器访问
- C3 远端节点通过 HTTP POST 将传感器数据传入 P4 主控，共享至 LVGL UI 显示

### 2.2 硬件系统介绍

#### 2.2.1 硬件整体介绍

本系统硬件以 ESP32-P4 为核心，主要硬件组成如下：

| 模块 | 型号/规格 | 接口 |
|------|----------|------|
| 主控 | ESP32-P4 | 板载 |
| 显示 | TFT + ILI9881C 驱动 | MIPI DSI 2-lane |
| 触摸 | GT911 | I2C |
| 摄像头 | OV5647 | MIPI CSI 2-lane |
| 音频 | ES8311 | I2S + I2C |
| 温湿度 | DHT22 | 单总线 |
| 光照 | BH1750 | I2C |
| 旋钮 | EC11 | GPIO |
| 按键 | BOOT 按键 | GPIO |
| 风扇驱动 | L9110S H 桥 | GPIO |
| 背光 | PWM 调光 | GPIO |

#### 2.2.2 机械结构

（此处插入实物装配照片）

系统采用 PCB 叠层 + 铜柱支撑的开放式结构，各模块通过排针排母连接，便于调试和更换。7 寸显示屏通过 FPC 排线连接至主板的 MIPI DSI 接口。摄像头模组通过 FPC 排线引出至机壳前方。DHT22 和 BH1750 传感器模块通过杜邦线引出，便于放置到合适位置测量。

#### 2.2.3 电路各模块介绍

**主控最小系统：**

USB 5V -> LDO 3.3V -> ESP32-P4 VDD
               -> 32.768kHz RTC 晶振
               -> 40MHz 主晶振
               -> 16MB Flash (SPI) + 32MB PSRAM (Octal SPI)

**MIPI DSI 显示电路：**

ESP32-P4 MIPI DSI -> MIPI D-PHY LDO 2.5V -> ILI9881C LCD 驱动
  信号线: DSI_D0P/N (2-lane 数据), DSI_CLKP/N (时钟)
  控制线: LCD_RST (GPIO), Backlight_PWM (GPIO)

**摄像头电路：**

OV5647 -> MIPI CSI 2-lane -> ESP32-P4 CSI
  控制: SCCB (I2C, GPIO)
  电源: PWDN (GPIO, 低电平使能), RST (GPIO, 高电平工作)

**音频电路：**

ES8311 <-> I2S <-> ESP32-P4
  信号线: MCLK, BCLK, WS, DIN, DOUT
  控制: I2C 配置, PA_EN (功放使能)
  输入: 麦克风 (IN1P)
  输出: 喇叭 (SPK_OUT)

**电源树：**

USB 5V -> LDO 3.3V -> ESP32-P4, ES8311, GT911, 传感器
       -> LDO 1.8V -> ESP32-P4 部分 IO
       -> MIPI DSI LDO 2.5V -> D-PHY 供电
       -> 5V 直通 -> 风扇驱动, 背光 LED

### 2.3 软件系统介绍

#### 2.3.1 软件整体介绍

系统软件基于 ESP-IDF 5.5 开发框架，采用 C++ 编写，主要依赖以下组件：

| 组件 | 用途 |
|------|------|
| ESP-IDF 5.5 | 操作系统与硬件抽象层 |
| LVGL 9.3 | 嵌入式图形用户界面 |
| ESP-SR | 语音识别与唤醒词 |
| Opus 1.5 | 音频编解码 |
| cJSON | JSON 解析/生成 |

软件分层架构：

```
应用层: Application(状态机) | MCP Server | SmartHome Web
服务层: AudioService | LcdDisplay | ExtraScreens | Protocol
接口层: Board | AudioCodec | Display | Camera | Led | Backlight
驱动层: ESP-SR | LVGL | I2C | SPI | GPIO | WiFi | MIPI DSI/CSI
```

#### 2.3.2 软件各模块介绍

##### (1) Application 状态机模块

功能：系统核心协调器，管理设备生命周期状态转换和事件分发。

状态定义：

```
Unknown -> Starting -> Idle <-> Listening <-> Speaking
               |            ^
          Configuring ------+
               |
          Upgrading / Activating / FatalError
```

启动流程：

```
app_main()
  -> NVS 初始化
  -> Application::GetInstance().Start()
    -> Board 初始化硬件（显示、音频、按键、传感器、摄像头）
    -> AudioService 启动（创建音频输入/输出/编解码任务）
    -> WiFi 连接
    -> SmartHome Web Server 启动
    -> 协议连接（WebSocket 或 MQTT+UDP）
    -> 主事件循环
```

关键变量：
- `device_state_`：当前设备状态（Idle, Listening, Speaking 等）
- `event_group_`：FreeRTOS 事件组，用于异步事件通知

##### (2) 音频流水线模块

功能：实现全双工音频处理。

```
MIC -> AudioInput Task (VAD + AEC + 唤醒词)
    -> Encode Queue -> Opus Encoder -> Send Queue
    -> Protocol 层 -> 云端 AI 服务器
    -> Decode Queue -> Opus Decoder -> Playback Queue
    -> AudioOutput Task -> SPEAKER
```

关键参数：
- OPUS_FRAME_DURATION_MS = 60（每帧 60ms）
- MAX_DECODE_PACKETS_IN_QUEUE = 40（约 2.4 秒缓存）
- 采样率：输入 24kHz，输出 24kHz

##### (3) 显示模块

功能：基于 LVGL 9.3 构建多页面 GUI。

页面布局：

```
状态栏: WiFi 图标 | 时间 | 电量 | 音量 | 表情
内容区: TileView 多页面
  页面 1: 聊天界面（语音气泡样式）
  页面 2: 智能家居面板（传感器数据 + 风扇/灯光控制）
  页面 3: 传感器历史曲线（LVGL Chart，120 点滚动）
  页面 4: 天气显示
锁屏: 60s 空闲自动锁屏，触摸解锁
```

传感器数据刷新流程：

```
LCD 主循环每 ~33ms
  -> extra_screens_update_time() 更新时间/日期/IP
  -> 每 2 帧触发 extra_screens_update_sensor_data()
    -> 读取 dht22_temperature/humidity -> 更新本地温湿度标签
    -> 读取 bh1750_lux -> 更新光照标签
    -> 调用 smart_home_get_room2_sensors() 读取远端数据 -> 更新远端标签
    -> 自动控制逻辑
  -> 每秒 history_timer_cb() -> 写入 LVGL Chart
```

##### (4) 通信协议模块

功能：提供与云端 AI 服务器的双向通信。

WebSocket 协议：

```
设备 -> 服务器: Hello JSON -> AudioChannel Open -> Opus 帧 + JSON
服务器 -> 设备: Hello Response -> STT/TTS/LLM/MCP JSON + Opus 帧
```

MQTT+UDP 混合协议：

```
控制通道 (MQTT): Hello / Listen / Abort / MCP / Goodbye 消息
音频通道 (UDP): AES-CTR 加密 Opus 包
  包头: type(1B) | flags(1B) | len(2B) | ssrc(4B) | ts(4B) | seq(4B)
```

##### (5) MCP 服务器模块

功能：实现 JSON-RPC 2.0 标准的 MCP 服务器。

支持的 MCP 工具：

| 工具名称 | 功能 | 参数 |
|---------|------|------|
| speaker.volume | 设置音量 | volume(0-100) |
| system.brightness | 设置亮度 | brightness(0-100) |
| system.set_theme | 切换主题 | theme(light/dark) |
| system.get_status | 获取系统状态 | - |
| smart_home.bulb.turn_on/off | 开关灯光 | - |
| smart_home.fan.turn_on/off | 开关风扇 | - |
| smart_home.set_auto_mode | 设自动模式 | auto(true/false) |
| smart_home.get_sensor_data | 获取传感器数据 | - |

MCP 工具注册代码示例：

```cpp
mcp.AddTool("self.smart_home.bulb.turn_on",
    "Turn on the bulb",
    PropertyList(),
    [](const PropertyList &) -> ReturnValue {
        s_light_on = true;
        update_light_ui();
        return true;
    });
```

交互流程：

```
AI -> tools/list -> 设备返回工具列表
AI -> tools/call {name:"bulb.turn_on", args:{}} -> 设备执行 GPIO 操作 -> 返回结果
```

##### (6) Smart Home Web 服务器模块

功能：在 SoftAP 模式下提供 Web 控制界面和 REST API。

REST API 端点：

| 端点 | 方法 | 功能 |
|------|------|------|
| / | GET | Web 控制面板主页 |
| /api/sensors | GET | 获取本机传感器数据 |
| /api/status | GET | 获取设备状态 |
| /api/fan | POST | 控制风扇 |
| /api/light | POST | 控制灯光 |
| /api/mode | POST | 切换自动/手动模式 |
| /api/weather | GET | 获取天气数据 |
| /api/room2/sensors | POST | C3 远端节点上报数据 |
| /api/room2 | GET | 获取远端传感器数据 |
| /api/camera/snapshot | GET | 获取摄像头 JPEG 快照 |

##### (7) 移动侦测模块

功能：基于帧差法的本地移动侦测，结合肤色检测定位人脸。

```
流程:
Capture() -> RGB565 帧 -> 转灰度图
DetectMotion():
  帧差法: |当前灰度 - 上一帧灰度| > 45 判定为变化像素
  变化率 > 5% -> 判定运动
  肤色检测 (R>50, G>20, R-G>5) -> 统计质心 -> 人脸框
  运动 + 肤色 -> GPIO 闪烁 500ms + 播放 PCM 音频
参数: 检测间隔 300ms, 触发冷却 10s
```

##### (8) ESP32-C3 远端传感器节点

功能：独立的远端采集节点，通过 HTTP 上报数据。

C3 端传感器上报代码：

```c
static void send_sensor_data(float temp, float humid, float lux)
{
    cJSON *root = cJSON_CreateObject();
    cJSON_AddNumberToObject(root, "temperature", temp);
    cJSON_AddNumberToObject(root, "humidity", humid);
    cJSON_AddNumberToObject(root, "light", lux);
    char *json = cJSON_PrintUnformatted(root);
    cJSON_Delete(root);

    esp_http_client_config_t cfg = {
        .url = "http://192.168.1.69/api/room2/sensors",
        .method = HTTP_METHOD_POST,
        .timeout_ms = 5000,
    };
    esp_http_client_handle_t client = esp_http_client_init(&cfg);
    esp_http_client_set_header(client, "Content-Type", "application/json");
    esp_http_client_set_post_field(client, json, strlen(json));
    esp_http_client_perform(client);
    esp_http_client_cleanup(client);
    cJSON_free(json);
}
```

P4 端接收代码：

```cpp
static esp_err_t room2_sensors_post_handler(httpd_req_t *req)
{
    char buf[256];
    int len = httpd_req_recv(req, buf, sizeof(buf) - 1);
    buf[len] = '\0';

    cJSON *root = cJSON_Parse(buf);
    cJSON *temp = cJSON_GetObjectItem(root, "temperature");
    cJSON *humid = cJSON_GetObjectItem(root, "humidity");
    cJSON *light = cJSON_GetObjectItem(root, "light");

    std::lock_guard<std::mutex> lock(s_room2_mutex);
    if (cJSON_IsNumber(temp)) s_room2_temp = temp->valuedouble;
    if (cJSON_IsNumber(humid)) s_room2_humid = humid->valuedouble;
    if (cJSON_IsNumber(light)) s_room2_light = light->valuedouble;
    s_room2_last_update = esp_timer_get_time() / 1000000;

    cJSON_Delete(root);
    httpd_resp_sendstr(req, "{\"success\": true}");
    return ESP_OK;
}
```

---

## 第三部分 完成情况及性能参数

### 3.1 整体介绍

（此处插入实物照片：正面照、斜 45 度全局照）

### 3.2 工程成果

#### 3.2.1 结构成果

（此处插入实物照片）

#### 3.2.2 电路成果

（此处插入实物照片：主板、接线细节）

#### 3.2.3 软件成果

（此处插入界面照片：聊天界面、智能家居面板、传感器曲线、天气显示）

### 3.3 特性成果

| 功能 | 测试结果 | 说明 |
|------|---------|------|
| AI 语音对话 | 唤醒率 > 95% | 全双工延迟 < 1s（局域网） |
| 本地传感器采集 | 精度 ±0.5°C / ±2%RH | DHT22 + BH1750 |
| 远端传感器传输 | 成功率 > 99% | 5s 周期 HTTP |
| 风扇控制 | 响应 < 50ms | L9110S H 桥驱动 |
| 灯光控制 | 响应 < 50ms | GPIO MOS 管驱动 |
| 自动模式 | 温度 > 28°C 开风扇 | 光照 < 10lux 开灯 |
| 移动侦测 | 300ms 间隔 | 帧差法 + 肤色检测 |
| MCP 工具调用 | 支持 8+ 工具 | JSON-RPC 2.0 |
| OTA 升级 | 支持版本检查 + 回滚 | 固件远程更新 |
| Web 控制 | 响应 < 100ms | SoftAP REST API |

---

## 第四部分 总结

### 4.1 可扩展之处

1. 可扩展 PM2.5、CO2、人体红外等传感器，构建更全面的环境感知网络
2. 基于 ESP-NOW 或 Zigbee 扩展多个 C3 节点，实现全屋环境监测覆盖
3. 利用 ESP32-P4 的 AI 加速器，在端侧运行轻量级语音识别或图像分类模型
4. 扩展为 MJPEG 或 H.264 视频流推送
5. 通过 MQTT 桥接接入 Home Assistant 智能家居生态
6. 蓝牙 Mesh 网关接入 BLE 设备网络

### 4.2 心得体会

本项目源于对大语言模型与嵌入式系统融合的兴趣。市面上的 AI 语音助手设备要么功能单一，要么价格高昂，我们希望通过低成本嵌入式平台实现一个将语音、视觉、触控和 IoT 控制融合为一体的智能家居终端。

**LVGL 左右滑动割裂感与卡顿的解决**

在 TileView 多页面切换时，左右滑动存在明显的割裂感和掉帧。排查发现两个原因：一是 LVGL 刷新缓冲区过小，导致一次滑动需要多次刷屏；二是传感器数据刷新函数 extra_screens_update_sensor_data() 在滑动动画过程中执行 GPIO 读写操作，阻塞了 UI 任务。

解决方案：将 LVGL 的 draw buffer 从单 buffer 改为双 buffer（double buffering），一个 buffer 用于前台显示，另一个用于后台绘制，交换时使用 DMA 传输，消除了撕裂感。同时将传感器数据刷新频率降低为每 2 帧执行一次（原为每帧），并在刷新函数中去掉阻塞式 GPIO 操作，改为设置标志位延后执行。

```cpp
// 双缓冲配置
static lv_draw_buf_t draw_buf_;
static lv_color_t *buf1 = (lv_color_t*)heap_caps_malloc(
    buf_size, MALLOC_CAP_SPIRAM | MALLOC_CAP_8BIT);
static lv_color_t *buf2 = (lv_color_t*)heap_caps_malloc(
    buf_size, MALLOC_CAP_SPIRAM | MALLOC_CAP_8BIT);
lv_draw_buf_init(&draw_buf_, buf1, buf2, buf_size, ...);

// UI 主循环降低传感器刷新频率
static int sensor_tick = 0;
sensor_tick++;
if (sensor_tick >= 2) {  // 每 2 帧刷新一次
    sensor_tick = 0;
    extra_screens_update_sensor_data();
}
```

**摄像头运动检测卡顿的解决**

摄像头移动侦测在 300ms 间隔下运行时，发现两个问题：一是每帧 malloc/free 缓存导致堆碎片化和性能抖动；二是帧差法全图遍历 640x480 灰度图时 CPU 占用过高，影响了音频 I2S 传输导致声音断续。

解决方案：将帧缓存、灰度缓存、肤色掩码等内存预分配为持久化缓存，避免每帧重复 malloc/free。同时通过调整检测间隔从 300ms 放宽到 500ms，并减少不必要的全图遍历区域（只扫描上半部分用于人脸检测），在检测效果不变的前提下大幅降低 CPU 占用。

```cpp
// 预分配持久化缓存（构造函数中一次性分配）
skin_mask_ = (uint8_t*)heap_caps_malloc(ftotal, MALLOC_CAP_SPIRAM | MALLOC_CAP_8BIT);
labels_ = (int*)heap_caps_malloc(ftotal * sizeof(int), MALLOC_CAP_SPIRAM | MALLOC_CAP_8BIT);

// 仅扫描画面上半部分用于肤色检测（人脸一般在上方）
int scan_h = (int)(fh * 0.40f);
for (int y = 0; y < scan_h; y++) {
    for (int x = 0; x < fw; x++) {
        // 肤色检测逻辑
    }
}
```

**AI 对话与其他语音播报的冲突处理**

系统运行时会有三类语音需要播放：AI 对话语音、温湿度定时播报、摄像头移动侦测提示音。这三者如果同时播放会导致音频混乱。解决策略：温湿度播报前检查设备状态，仅当 Idle 状态下才播报；摄像头播报 "你好主人" 前同样检查设备状态，AI 说话时（Listening/Speaking 状态）跳过提示音。

```cpp
// 温湿度播报前检查状态
if (app->device_state_ != kDeviceStateIdle) return;
app->AnnounceTemperature();

// 摄像头播报前检查状态（伪代码示意）
int64_t now = esp_timer_get_time() / 1000000;
if (Application::GetInstance().GetDeviceState() == kDeviceStateIdle
    && now - last_speak_time > 10) {
    last_speak_time = now;
    PlayPCM("你好主人"); // 仅在空闲时播放
}
```

**其他调试经验：**

PSRAM 内存不足通过复用帧缓存和编译优化解决；I2S 音频时钟抖动需要精确的 24.576MHz 时钟，通过配置 ESP32-P4 的 APLL 输出解决；WiFi 传输大块数据时导致音频断流，通过启用 DMA 传输音频数据并提高音频任务优先级解决。

通过本项目，我们认识到即使是资源受限的 MCU，只要有合理的架构设计，也能成为 AI 时代具有交互能力的物理节点。MCP 协议的落地是最大的收获——标准化的工具描述和调用机制让 AI 模型无需针对特定硬件编写适配代码即可操纵物理设备，这是未来智能家居设备互联互通的重要方向。

---

## 第五部分 参考文献

[1] Espressif Systems. ESP32-P4 Technical Reference Manual, 2024.
[2] Espressif Systems. ESP-IDF Programming Guide v5.5.
[3] LVGL. Light and Versatile Embedded Graphics Library. https://lvgl.io/
[4] Valin, J.M., et al. Definition of the Opus Audio Codec. RFC 6716, IETF, 2012.
[5] JSON-RPC 2.0 Specification. https://www.jsonrpc.org/specification
[6] Model Context Protocol Specification. https://modelcontextprotocol.io/
[7] Espressif Systems. ESP-SR Speech Recognition Framework.
[8] DHT22 Datasheet. Aosong Electronics, 2020.
[9] ROHM Semiconductor. BH1750FVI Ambient Light Sensor Datasheet.
[10] FreeRTOS. Real-Time Operating System for Microcontrollers. https://www.freertos.org/
